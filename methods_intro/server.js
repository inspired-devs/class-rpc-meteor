/*
if (Meteor.isServer) {
  Meteor.methods({
    '/say/hello': function (name) {
      return 'Hello, ' + name;
    }
  });

  var result = Meteor.call('/say/hello', 'Chris');
  console.log('Result from Server: ' + result);
}
*/
