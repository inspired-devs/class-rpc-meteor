/*
if (Meteor.isClient) {
  Meteor.methods({
    '/say/hello': function (name) {
      console.log('Running the Meteor method stub on the client');
    }
  });

  Meteor.call('/say/hello', 'Chris', function (err, result) {
    console.log('Result from Server: ' + result);
  });
}
*/
